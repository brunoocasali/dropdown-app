<%= f.fields_for :installments do |fi| %>

  <b>Installment:</b> <%= ": #{fi.options[:child_index] + 1} of #{account.installments.size} " %>
  <div class="field">
    <%= fi.label :amount %><br>
    <%= fi.text_field :amount %>
  </div>
  <div class="field">
    <%= fi.label :number %><br>
    <%= fi.number_field :number %>
  </div>
<% end %>
